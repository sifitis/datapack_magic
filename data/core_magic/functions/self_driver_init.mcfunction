
#This is a list of all functions that are triggered at startup and then schedule their own continuation, thus being self propgating.
#This is mostly used for functions that don't need to be run every tick.

function core_magic:trigger_control